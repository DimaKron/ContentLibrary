<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.kphu.itis.contentlibrarytest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DialogContentActivity"
            android:screenOrientation="portrait"
            android:label="@string/dialog_content_title"/>

        <activity android:name=".ActivityContentActivity"
            android:screenOrientation="portrait"
            android:label="@string/activity_content_title"/>

        <activity android:name=".ViewContentActivity"
            android:screenOrientation="portrait"
            android:label="@string/view_content_title"/>
    </application>

</manifest>