<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.kphu.itis.contentlibrary">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".activity.DefaultContentActivity"
            android:label="@string/default_activity_label"/>

        <activity android:name=".content_instruments.image_gallery.DefaultImageGalleryActivity"
            android:label="@string/default_image_gallery_activity_label"/>

    </application>

</manifest>
